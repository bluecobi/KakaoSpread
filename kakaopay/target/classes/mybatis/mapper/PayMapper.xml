<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ljk.pay.mapper.PayMapper">
    <select id="getMaxSequenceId" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT paydb.nextval(#{tableName})
    </select>
</mapper>
